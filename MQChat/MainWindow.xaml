<Window x:Class="MQChat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:MQChat"
        mc:Ignorable="d"
        Title="MQChat"
        Height="600" MinHeight="300"
        Width="800" MinWidth="400">

    <hc:Watermark Mark="GSC🤣 " FontSize="15">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" MinWidth="100" MaxWidth="300"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!--左侧联系人列表-->
            <Border Grid.Column="0" Height="30" VerticalAlignment="Top" CornerRadius="5" Background="{DynamicResource SuccessBrush}">
                <TextBlock Foreground="{DynamicResource RegionBrush}" Text="{x:Static local:GlobalData.myName}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
            <ListBox Grid.Column="0" Margin="0,40,0,0" ItemsSource="{x:Static local:GlobalData.userCustoms}" SelectionChanged="ListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CustomName}" ToolTip="{Binding UserID}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Grid.Column="0" Width="40" Height="40" Margin="5"
                    VerticalAlignment="Bottom" HorizontalAlignment="Right"
                    Style="{StaticResource ButtonIcon}"
                    Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource RegionBrush}"
                    hc:IconElement.Geometry="{StaticResource AddGeometry}" hc:BorderElement.CornerRadius="20"/>
            <!--竖排分割栏-->
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <!--右侧收发消息区域-->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="100" MinHeight="70" MaxHeight="300"/>
                </Grid.RowDefinitions>
                <!--顶部聊天列表-->
                <ListBox Grid.Row="0" Name="msgListBox">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding MsgContent}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <!--横向分割栏-->
                <GridSplitter Grid.Row="1" Height="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                <!--底部消息编辑区域-->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="75"/>
                    </Grid.ColumnDefinitions>
                    <RichTextBox Grid.Column="0"/>
                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" BorderThickness="0" Content="发送" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="auto"
                                Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource RegionBrush}"/>
                        <Button Grid.Row="2" BorderThickness="0" Content="附件" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="auto"
                                Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource RegionBrush}"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </hc:Watermark>
</Window>
